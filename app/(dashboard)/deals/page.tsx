// DealsBoard (in deals/layout.tsx) now owns the deals table permanently.
// This page segment must exist for Next.js routing but renders nothing.
export default function DealsPage() { return null; }
